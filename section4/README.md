# Section 4

## Task 4.1

SFS weights source-sensitive tokens higher than ordinary tokens and measures exact preservation.

`SFS = preserved_weight / total_source_weight`.

Examples and implementation are in `sfs.py` and tests.
